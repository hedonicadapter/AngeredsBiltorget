import { atom } from 'nanostores';

export const CTAHovered = atom(false);

export const sidebarOpen = atom(false);

export const filterContainerExpanded = atom(false);

export const lastCarInView = atom(false);
