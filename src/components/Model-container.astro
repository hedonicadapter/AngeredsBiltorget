---
// import ThreeDeeModel from './ThreeDeeModel';
import FakeThreeDeeModel from './FakeThreeDeeModel';
import {ScrollTransitionWrapper} from './MotionComponents';

---

<div id="fake-model-container">
    <ScrollTransitionWrapper client:only opacityRate={0.15} xRate={0.15} xDistance={-500}>
        <FakeThreeDeeModel slot="child" transition:persist client:load/>
    </ScrollTransitionWrapper>
    <!-- <ThreeDeeModel transition:name="model" transition:persist client:only/> -->
</div>